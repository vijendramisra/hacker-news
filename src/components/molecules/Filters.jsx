import React from "react";
import Button from "../atoms/Button";
const Filters = () => {
    return (
        <>
            <Button>top</Button>
            <Button>new</Button>
        </>
    );
};

export default Filters;
